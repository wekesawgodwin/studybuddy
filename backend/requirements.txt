# Web framework
fastapi==0.111.0
uvicorn==0.29.0

# Database — PostgreSQL
sqlalchemy==2.0.29
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication — JWT
python-jose[cryptography]==3.3.0

# Password hashing — used to hash the OTP before storing it
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Email — Resend HTTP API (works on all cloud providers including Railway)
resend==2.0.0

# Environment variables
python-dotenv==1.0.0